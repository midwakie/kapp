export type CheckBoxProps = {
  borderWidth: number;
  height: number;
  width: number;
  color: string;
  icon: string;
  iconSize: number;
  iconColor: string;
  onChange?: Function;
  status: boolean;
};
